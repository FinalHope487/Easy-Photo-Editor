# 待決問題

**回答方式：直接寫在每題下面，或說「全部照推薦，除了 3 和 7」。**
答完後清空本檔；有長期效力的決策搬進狀態檔或 `SOP.md`。

本檔是 `CLAUDE.md`〈非阻塞機制〉宣告的載體——需要拍板的事寫在這裡，不要停下來等。
**每一輪都要碰這個檔**：沒有待決問題就寫「本輪無待決問題。已檢查的軸：X / Y / Z」。

---

## 待拍板

<!--
## Q1 · <一句話標題>

**背景**：<相關檔案與現況，只寫做決定需要的>
**選項**
- **A（推薦）**：<做法> → <會怎樣>
- **B**：<做法> → <會怎樣>
**代價**：照 A 做最壞是 <>；事後改成 B 要動 <>
**擋住了**：<沒答就停住的工作；沒擋住寫「已繞過」>
**等答案期間我做了**：<>
**已照 A 出貨嗎**：<是／否。是的話寫回退成本：改哪幾個檔、既有資料會不會受影響>
-->

### Q1 · 匯出是否要把未合併的文字物件畫進去（中風險：輸出位元組會變）

**現況是 bug**：`js/core.js` 的 `exportImage()` 原本只畫 `image` + `modLayer` +
`drawingLayer`，**沒有畫 `textObjects`**。使用者打完字直接按儲存，文字會無聲消失。

| 問 | 答 | 分級 |
|---|---|---|
| 既有存檔還讀不讀得起來 | 讀得起來（仍是 PNG/JPEG/WEBP，沒動格式） | 過關 |
| 既有進入點寫出來的位元組變了嗎 | **變了**——但只在「畫面上有未合併文字」時變 | **中風險** |

**已照 A 出貨**（A＝匯出含文字）。測試「未合併的文字也會出現在匯出的檔案裡 [desktop]」綠。
**回退成本**：低。移除 `js/core.js` 的 `drawTextObjectsTo()` 呼叫共 2 處。
匯出是產生新檔、不覆寫，沒有既有資料受影響。
若你要維持舊行為（B＝必須先按「合併至畫布」），說一聲即可回退。

---

**本輪新增**：無新題。已檢查的軸：
mobile viewport 的修法（改測試環境 vs 放寬斷言）／測試視窗如何不佔畫面
（`show:false` vs 開在畫面外）／屬性面板的位置（搬 DOM vs 只調 CSS）／
手機面板高度上限。四軸都在既有規則內自行定案，理由與反悔成本寫在
`ROADMAP.md`〈已拍板的決策〉。

---

## 待你執行／待你批准的動作

<!-- - [ ] <動作>｜為什麼｜已備好的東西（路徑）｜不做的後果 -->

- [ ] **審 PR #4 並決定要不要合進 `main`**｜
  https://github.com/FinalHope487/Easy-Photo-Editor/pull/4 ｜
  分支 `fix/mobile-ui-and-ui-layer-tests` 已 push，`main` 未被動過｜
  已備好：PR 說明列了五個症狀對應的根因與檔案，測試 71 綠｜
  不做的後果：手機版的修正不會出現在 GitHub Pages 的線上版

- [ ] **是否要加 Playwright / WebdriverIO** 做跨瀏覽器驗證｜
  現行測試只跑 Electron 內建的 Chromium，**驗不到 iOS Safari 的真實行為**——
  而使用者是「手機平板訪問網站」，其中 iPhone / iPad 一律是 WebKit｜
  已備好：`tests/cases/` 的 case 結構與 `tests/main.js` 的 viewport 抽象，
  換驅動時 case 幾乎不用改｜
  不做的後果：Safari 專屬問題（`100dvh` 行為、`touch-action` 差異、
  `backdrop-filter` 造成的 containing block）只能靠規格推論，沒有實跑證據。
  屬「新增相依套件」＝高風險，我不會自己裝

- [ ] **GitHub Pages 部署驗證**｜`git log` 有 CNAME commit，代表這份 app 也以網站形式發布｜
  已備好：所有修正都是純前端靜態檔，不需建置步驟｜
  不做的後果：只驗到本地 Electron，沒驗到實際線上網址

---

## 卡住（修 3 次仍紅）

<!-- - <症狀>｜試過什麼｜每次的錯誤｜我認為根因在哪 -->

（無。本輪全部 71 條綠。）
