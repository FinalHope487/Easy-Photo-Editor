# 待決問題

**回答方式：直接寫在每題下面，或說「全部照推薦，除了 3 和 7」。**
答完後清空本檔；有長期效力的決策搬進狀態檔或 `SOP.md`。

本檔是 `CLAUDE.md`〈非阻塞機制〉宣告的載體——需要拍板的事寫在這裡，不要停下來等。
**每一輪都要碰這個檔**：沒有待決問題就寫「本輪無待決問題。已檢查的軸：X / Y / Z」。

---

## 待拍板

**本輪無待決問題。** 已檢查的軸：三個 PR 的合併順序（依序合 vs 由下往上合成一次部署）
／被誤關的 PR 要救回還是重開一個新的（選救回，保住既有說明與討論）
／合併後的分支要不要留（刪掉，`main` 與 `feat/add-text-and-eraser-tool` 之外全清）。

---

## 待你執行／待你批准的動作

<!-- - [ ] <動作>｜為什麼｜已備好的東西（路徑）｜不做的後果 -->

（無。上一批四項全部結案：）

- [x] PR #4 / #5 / #6 已依序合進 `main`（`78d3e2d`）
- [x] `npm run test:pages` → **8 passed, 0 failed**，線上版拿到全部修正
- [x] `npm audit` → found 0 vulnerabilities
- [x] WebKit 驗證層已接進 `npm test`

---

## 卡住（修 3 次仍紅）

<!-- - <症狀>｜試過什麼｜每次的錯誤｜我認為根因在哪 -->

（無。`main` 上實跑 `npm test` 80 passed / 0 failed / 0 skipped。）
